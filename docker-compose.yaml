services:
  uptime:
    build: .
    restart: unless-stopped
    ports:
      - "3069:3069"
    environment:
      - DATABASE_URL=file:/app/data/uptime.db
      - ADMIN_PASSWORD=changeme
      - PORT=3069
